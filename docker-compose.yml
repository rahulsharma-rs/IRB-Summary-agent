version: "3.9"

services:
  irb_agent:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: irb-agent
    ports:
      - "8502:8502"
    env_file:
      - .env
    environment:
      STREAMLIT_SERVER_BASEURLPATH: "/irb"
      STREAMLIT_SERVER_ENABLEXSRFPROTECTION: "false"  # Add this
    volumes:
      - rag_cache:/app/.rag_cache
      - uploads:/app/uploads
      - ./.streamlit:/app/.streamlit  # Mount config
    restart: unless-stopped

volumes:
  rag_cache:
  uploads: