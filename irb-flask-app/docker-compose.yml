version: "3.9"

services:
  irb_flask:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: irb-flask
    env_file:
      - .env
    ports:
      - "5000:5000"
    volumes:
      - ./:/app
    restart: unless-stopped
